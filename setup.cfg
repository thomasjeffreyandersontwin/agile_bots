[metadata]
name = agile_bots
version = 0.1.0

[options]
packages = find:
python_requires = >=3.10
install_requires =
    fastmcp>=0.2.0
    nltk>=3.8.0

[options.packages.find]
where = .
include = agile_bot*
exclude = test*, legacy*, backups*
